spring:
  datasource:
    url: jdbc:postgresql://host.minikube.internal:5435/series-db
    username: postgres
    password: 1234

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery.default.svc.cluster.local/eureka/